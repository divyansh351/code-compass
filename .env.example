# Code Compass Environment Configuration Example
# Code Compass is local-first: no external credentials are required for standard deterministic operation.

# Optional: Ollama configuration (if using local Ollama instance)
# OLLAMA_HOST=http://localhost:11434
# OLLAMA_MODEL=llama3

# Optional: Log Level (DEBUG, INFO, WARNING, ERROR)
COMPASS_LOG_LEVEL=INFO
